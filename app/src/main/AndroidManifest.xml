<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.loginkt">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".data.ui.main.activity.SignUpActivity"
            android:windowSoftInputMode="stateHidden|adjustPan"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".data.ui.main.activity.SignInActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".data.ui.main.activity.admin.MainActivityAdmin"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".data.ui.main.activity.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".data.ui.main.activity.PenginapanActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".data.ui.main.activity.ProductActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".data.ui.main.activity.DetailProductAcitivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>